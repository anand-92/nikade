# 2026-03-19 工作日志

## Session 1

### 完成

**WindowServer 压垮问题根因分析和修复**（核心修复，6 文件）
- 根因：所有已激活的终端 tab 的 Metal surface 通过 `opacity(0)` 保留在 ZStack 中，持续向 WindowServer 提交 drawables，导致 WindowServer CPU 飙升
- 修复方案（双重保护）：
  - `TerminalWorkspaceView` — 只渲染 `activeTabID` 的 tab，移除 `activatedTabIDs` 状态
  - `TerminalNSView` — 添加 `setRenderingPaused`/`setSurfaceVisibility`，通过 `metalLayer.isHidden` 控制
  - `TerminalPanel` — 添加 `isVisible` 参数，在 `updateNSView` 中实时更新
  - `TerminalScrollView` — 添加 `setTerminalVisibility` 方法
  - `ContentView` — 传递 `isVisible: navigationStore.activeTab == .terminal`
- 修改文件：GhosttyTerminal.swift, GhosttyApp.swift, TerminalPanel.swift, TerminalScrollView.swift, TerminalWorkspaceView.swift, ContentView.swift

**Code Review 修复**（两个并行 review agent）
- 删除死代码 `updateVisiblePanes`（GhosttyAppManager）
- `setSurfaceVisibility` 加 guard 避免重复调用
- `makeBackingLayer` 中立即应用 `hostVisible` 状态
- `viewDidMoveToWindow` 重连时用 `hostVisible` 保护 focus 设置
- 更新 `setRenderingPaused` 注释（诚实说明 ghostty 内部仍可能渲染）

**Health check 指数退避优化**
- DeploymentStore health check 连续失败时从 30s 逐步退避到 5 分钟
- 服务恢复后自动回到 30s
- 减少无效 TCP 连接和 console 噪音
- 修改文件：DeploymentStore.swift

### 决策

- 选择 `metalLayer.isHidden` 方案而非移除 view hierarchy — 避免重新挂载时的视觉闪烁
- 最终采用"只渲染 active tab"+ `metalLayer.isHidden` 双重保护
- ghostty 内部渲染无法从 Swift 侧完全暂停（需 C API 支持），当前修复已解决 WindowServer 合成压力
- health check 退避选择指数退避而非完全停止 — 更用户友好

### 进行中

- 用户还未实际测试 Metal visibility fix 效果（需切换 Cmd+1/Cmd+2 观察 `[Metal]` 日志和 WindowServer CPU）
- Debug 日志 `#if DEBUG NSLog` 已加入 `setSurfaceVisibility`，验证后可删除

## Session 2

### 完成

**Code Review 三轮并行修复**
- Code Reviewer: consecutiveHealthFailures 清理、layout() 冗余调用
- Code Simplifier: 6 项精简建议（部分由 linter 自动应用）
- Silent Failure Hunter: task_info 错误提示、health state 清理、@ObservationIgnored 确认正确

**@ObservationIgnored 修复 surface 泄漏**
- GhosttyAppManager 内部字典（paneSurfaceMap, paneViewMap, retainedTerminalViews 等）标记为 @ObservationIgnored
- 防止 SwiftUI 级联 re-evaluation 创建幽灵 surface
- 验证：8 项目 + 8 分屏 = 15 total，精确匹配无泄漏

**Debug 诊断系统**
- ⌘⇧I 一键复制诊断信息到剪贴板（通过 Debug 菜单）
- StatusBarView 状态栏 Metal X/Y 指标（2 秒轮询，仅 DEBUG）

**项目 Tab 关闭 + 拖拽排序**
- hover 显示 ✕ 关闭按钮（从 Button 改为 onTapGesture 解决和 onDrag 的冲突）
- onDrag/onDrop + ProjectTabDropDelegate 实现拖拽排序
- ProjectStore.moveRootProject 方法，worktree 跟随 root 移动

**Worktree 自动发现**
- addOrActivateProject 时自动调用 git worktree list
- 已存在的 worktree 自动添加到项目列表

**CodeEditSourceEditor fork 修复**
- project.yml 中指向 sanvibyfish/CodeEditSourceEditor fork
- 修复 MinimapView brightnessComponent crash
- xcodegen generate 不再重置依赖

**v1.0.3 发布**
- 版本号 1.0.3 build 4
- DMG 打包 + Apple 公证 + Gatekeeper 通过
- GitHub Release 创建并上传 DMG
- Release notes 撰写

**Mori 项目分析**
- 对比 openOwl vs Mori 架构差异
- 评估 6 个借鉴方向的适用性

### 决策

- **tmux 持久化不做** — ghostty 是 shell，tmux 是用户自由选择
- **LRU cache 不适用** — openOwl 的 surface=进程，evict=杀进程，当前 metalLayer.isHidden 方案更好
- **TerminalHost 协议不做** — 强耦合 ghostty 是合理的
- **GRDB 不做** — JSON 可读性更重要
- **模块化优先拆 Git 和 File** — 这两个最独立，其他耦合高的先不动
- **模块化预估 3-4 天** — 不急，放到后续 session

### 进行中

- Git 和 File 模块化（待下个 session 开始）
