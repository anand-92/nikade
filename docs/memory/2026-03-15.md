# 2026-03-15 工作日志

## 06:55 (checkpoint)

### 完成

- **terminfo 缺失修复**：发现 `setup.sh` 只复制了 `zig-out/share/ghostty/`（shell-integration/themes/doc），漏掉了 `zig-out/share/terminfo/`。修复了 setup.sh 并手动补充缓存。在 surface 创建时注入 `TERMINFO_DIRS` 环境变量指向 `ghostty-resources/terminfo`。验证 `tput colors` = 256，`echo $TERM` = xterm-ghostty
- **鼠标 Y 坐标翻转**：AppKit 左下角原点 → Ghostty 左上角原点，所有 `ghostty_surface_mouse_pos` 调用改为 `bounds.height - point.y`（对齐 cmux）
- **Clipboard 粘贴 crash 修复**：Cmd+V 通过 `ghostty_surface_key` 触发 paste binding → `read_clipboard_cb` → `ghostty_surface_complete_clipboard_request` 重入 crash。改为在 `performKeyEquivalent` 拦截 Cmd+V/C，直接用 `ghostty_surface_text` 粘贴
- **IME 组合输入 workaround**：composing 时跳过 `keyAction`（key encoder 在 composing=true 时仍泄漏首字符）；preedit SET 恢复（terminfo 修复后 autosuggestion 颜色正常，不再造成视觉翻倍）
- **Clipboard write 回调修复**：对齐 cmux 实现，遍历 `UnsafeBufferPointer` 查找 text/plain MIME
- **Worktree 路径修复**：`~/.openowl/workspace/projects/<projectName>/<strippedBranch>`，分支前缀自动去除（fix/abc → abc）
- **Sidebar 展开/收缩**：chevron 按钮切换项目展开状态 + sidebar.left 图标全收缩 + 分隔线 hover 显示 chevron 恢复
- **Sidebar 宽度拖拽**：拖拽时显示指示线，松手改宽度，拖到 <80px 自动收缩
- **分屏扁平布局重构**：用 `ZStack` + `ForEach(paneIDs)` + `.position()` 绝对定位替代递归 `HStack/VStack`，目标是树结构变化时不重建 terminal view
- **分屏 ratio 拖拽修复**：用 `@State dividerDragStart` 字典记住初始 ratio，避免 `DragGesture.translation` 累加
- **Pane 拖拽重排**：PaneDragHandle + PaneDropDelegate + drop zone 位置检测（left/right/top/bottom/center）

### 进行中

- **分屏布局显示问题**：扁平布局 `.position()` 定位基本工作，但可能存在 hit test 区域不精确的问题（drop zone 可能覆盖相邻 pane）。已加 `.clipped()` + `.contentShape(Rectangle())`，需进一步测试
- **终端重载问题**：分屏增删时 terminal 仍可能重载。扁平布局理论上应该避免（ForEach by paneID），但实际效果待验证
- **文件编辑器集成**：用户要求集成 CodeEditTextView 替代当前只读预览。尚未开始

### 决策

- **键盘处理不再魔改**：严格对齐 Ghostty SurfaceView_AppKit.swift 源码，只保留 2 个必要 workaround（composing skip + preedit 恢复后的 clear-only fallback）
- **terminfo 通过环境变量注入**：在 `ghostty_surface_config` 的 `env_vars` 中设置 `TERMINFO_DIRS`，而非安装到 `~/.terminfo/`
- **Cmd+V/C 在 performKeyEquivalent 拦截**：避免通过 ghostty_surface_key 触发 paste binding 的重入 crash
- **扁平布局替代递归布局**：为解决分屏增删导致 terminal view 重建的问题

### 问题

- **"lsls" 翻倍根因**：最终确认不是键盘处理 bug，而是 zsh-autosuggestions 颜色渲染错误（SGR 38 裸序列），根因是 terminfo 缺失
- **SGR 38 警告**：`io_handler: unimplemented or unknown SGR attribute: .{ .full = { 38 }, .partial = { 38 } }` — 可能来自 oh-my-zsh 主题发送的裸 `\e[38m]` 序列，terminfo 修复后频率降低但仍偶尔出现
- **中文 IME composing 字符泄漏**：`ghostty_surface_key` 在 `composing=true` 时仍发送首字符到 PTY。Ghostty 源码和 cmux 同样发送 composing 事件但不泄漏，原因不明（可能与 libghostty 版本或 surface 初始化差异有关）
