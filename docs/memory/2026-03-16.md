# 2026-03-16 工作日志

## Checkpoint 1

### 完成

1. **Sidebar 展开/收缩修复** — 文件夹行移除 `.tag()`，不再参与 List selection；点击文件夹只做展开/收缩；焦点始终在分支行上；无 git 项目显示 "No commits yet"
2. **项目切换性能优化** — `syncActiveProjectContext()` 改为只刷新当前可见 tab 的 store（terminal/file/git），移除每次切换的 git branch fetch
3. **QuickOpen (Cmd+P) 修复** — 修复 `performKeyEquivalent` 的 firstResponder guard 导致 TextField 无法粘贴/复制；ESC 始终关闭面板；点击外部关闭；lazy-load 文件索引确保任何 tab 都能打开
4. **SwiftUI "publishing changes from within view updates" 修复** — listSelection.set 用 DispatchQueue.main.async 延迟
5. **CodeEditTextView Typesetter bug 修复** — `lineBreak` 是绝对位置但被当作 length，导致 CJK 换行文字重复。已提 PR: CodeEditApp/CodeEditTextView#122 (已有人提)
6. **CodeEditSourceEditor MinimapView hitTest bug 修复** — hidden minimap 仍拦截右侧点击。已提 Issue #370 和 PR #371
7. **编辑器 tab 切换/关闭修复** — ScrollView 内 .onTapGesture 被 NSClipView 吞掉，改用 Button；去掉 ScrollView 用 HStack
8. **编辑器 SourceEditor frame** — 添加 `.frame(maxWidth: .infinity, maxHeight: .infinity)` 确保填满
9. **Sidebar BranchRow 交互** — 添加 hover 复制路径图标和右键菜单，与 WorktreeRow 一致；对齐 icon+text 结构
10. **内联重命名 Finder 风格** — WorktreeRow 改 `.textFieldStyle(.plain)`；OutlineTreeCellView 去掉 roundedBezel 改淡蓝背景
11. **测试** — 新增 SidebarExpandCollapseTests (7 tests)，全部通过

### 进行中

- 用户提到想做整个界面的 UI review（还没开始）

### 决策

- 两个第三方库 bug (Typesetter + MinimapView hitTest) 直接修改 DerivedData 的 SPM checkout，已提 PR 给上游
- Debug 日志 (HITTEST monitor, CLICK/OFFSET logs) 暂时保留
- Deployment 表单的 `.roundedBorder` 保持不动（正式表单 vs 内联重命名）

## Checkpoint 2

### 完成

1. **docs/features/README.md + 全套功能文档** — 创建 001-006 编号文档（libghostty/终端分屏/Git变更/文件浏览器/本地部署/项目侧边栏），含 README 索引
2. **Swift Testing 基础设施** — project.yml 添加 openOwlTests target + schemes，149 个测试全部通过（14 suites）
3. **ProjectStore 持久化迁移** — UserDefaults → `~/.openowl/openowl.json`，含 UserDefaults 一次性迁移
4. **GitService 可见性调整** — `private extension` → `extension` 使解析函数可被测试访问
5. **FileExplorerStore 可见性调整** — 同上，开放 static 方法给测试
6. **Cmd+P Quick Open 修复** — 终端 performKeyEquivalent 拦截 Cmd+P，通过 Notification 触发，不被 ghostty keyDown 吞掉
7. **文件切换缓存优化** — setProject() 有缓存时跳过浅扫描，只做 refreshFullOnly()，避免 16034 节点树被 92 节点覆盖导致闪烁
8. **文件浏览器 3 个 bug 修复**:
   - onExpandDirectory 未连接 → lazy 目录无法展开
   - Git 状态只到第一层 → 目录显示 ● badge + lazy 目录用路径前缀推断 git 状态
   - ESC 不退出重命名 → 添加 onCancelRename 回调清除 renamingNodeID
9. **目录树自动收缩 bug** — 移除 outlineViewSelectionDidChange 里的 toggle 逻辑
10. **Git badge 布局溢出** — cell 布局改为右对齐，nameField compression resistance = low
11. **文件目录面板可拖拽** — 固定 width 改为 @State + DragGesture，150-500px 范围
12. **编辑器 tab close 按钮修复** — Button 改为 Image + onTapGesture 避免手势冲突
13. **编辑器内容溢出修复** — SourceEditor 加 .clipped()
14. **Git Graph commit diff 完整实现**:
    - GitService: commitFiles/showCommit/showCommitFile/fileData 方法
    - 按文件分 section（可折叠），类似 GitHub
    - 左侧文件列表 sidebar（可收缩，点击跳转）
    - 图片 diff 左右对比（git show ref:path 提取历史图片）
    - diff 空白侧颜色区分（淡红/淡绿）
    - LazyVStack → VStack 修复滚动定位不准
15. **MenuBar icon 处理** — 1024px 原图裁剪/反色/缩放到 18x18 + 36x36 template image

### 进行中

- 菜单栏快捷键发现性（P0：添加 NSMenu 菜单栏）— 已调研方案，未开始实现

### 决策

- 测试框架选择 Swift Testing 而非 XCTest
- TEST_HOST + BUNDLE_LOADER 让测试注入 app 进程，避免重复链接 libghostty
- splitDiffByFile 从 private 改为 internal 以支持测试
- Git Graph commit diff 用 VStack 而非 LazyVStack（文件数量有限，保证 scrollTo 定位准确）
