# 2026-05-10 工作日志

## 18:34 (checkpoint) — .dic 内存泄漏排查 + 1.0.8 打包

### 完成

- 排查 openOwl 严重内存泄漏，定位到 3 个 per-click 累积路径并修复：
  1. **`OutlineTreeViewController.swift:528-538`** — `menuNeedsUpdate` 里的 `selectRowIndexes` 没设 `isProgrammaticSelection`，导致右键文件触发 `selectionDidChange` → `onSelectFile` → `openFileInTab` 自动加载文件。修复：包一层 `isProgrammaticSelection = true / false`。
  2. **`FileExplorerView.swift`** — `tabStorages` / `tabImageCache` 无上限累积。修复：加 `maxOpenTabs = 10` 常量 + `evictOldestTabIfNeeded()`，淘汰最旧的 non-active non-dirty tab。
  3. **`FileExplorerStore.swift`** — `projectScanCache` 永不清理。修复：新增 `writeProjectScanCache(_:_:)` + `projectScanCacheOrder` 维护 LRU；`maxProjectScanCacheEntries = 5`。
- 主 build 通过（`** BUILD SUCCEEDED **`），test bundle 编译通过。

### 进行中

- 用户报告"单击 .dic 文件时内存涨 1GB+"，但路径不在已修的三处（.dic 1.4MB > 1MB cap 会被 `openFileInTab` 拒绝，理论上单次点击不该涨 1GB）。
- 加了一组临时诊断日志（`[DIAG-MEM]` tag）覆盖 `openFileInTab` 各分支 + `loadPreviewForSelection` + `selectNode`，使用 `mach_task_basic_info` 输出 resident MB。
- 待用户用打包好的 1.0.8 版本复现 .dic 单击场景，把 Console 里所有 `DIAG-MEM` 行回贴。
- 用户决定打 1.0.8 版本（保留诊断日志、走完整 build-dmg.sh 含公证 staple）。下一步：升 MARKETING_VERSION → commit → 跑 build-dmg.sh。

### 决策

- **三个 fix 一起打包**：用户在 AskUserQuestion 中选了"三个 bug 一起修（推荐）"。
- **保留 DIAG-MEM 诊断日志在 1.0.8 包里**：用户的 1GB+ 暴涨没有静态分析路径能解释，需要实测日志确认根因。诊断日志后续再清理。
- **升版本到 1.0.8**：当前在 `release/v1.0.8` 分支，但 `project.yml` 的 `MARKETING_VERSION` 仍是 1.0.7，需对齐。
- **未跑单元测试**：xctest bundle codesign 失败（"code object is not signed at all"），是签名环境问题，与代码改动无关。`build-for-testing CODE_SIGNING_ALLOWED=NO` 通过证明改动 compile 正确。

### 用户报告的关键信息

- 内存涨幅：+500MB 以上（实测 1GB+）
- .dic 路径：`~/Documents/workspace/web3/thai-learn-web/packages/core/src/thai-processor/data/`
- 实际文件大小：`th_TH.dic` = 1.4MB（>1MB cap，应被拒绝），`hyph_th_TH.dic` = 65KB（<1MB，会进 SourceEditor）
- 触发动作：单击（不是双击/右键），文件内容显示空白

### 还没动的下一步

- 修 MARKETING_VERSION 到 1.0.8（在 `project.yml`）
- commit 三个 fix + 诊断日志 + 版本号 bump
- 跑 `scripts/build-dmg.sh`（archive + export + DMG + sign + notarize + staple，~5-15 分钟）
