# 2026-03-18 工作日志

## Session 1

### 完成

**REQ-006 Claude 异常提醒**
- `ClaudeStatusStore` (@Observable) 轮询 `status.claude.com/history.rss`，间隔 2 分钟
- 异常时在 Sidebar 底部显示可关闭提醒卡片
- 已关闭的 incident key 持久化到 `UserDefaults`
- 提交：`d5fb73a`

**Sidebar PaneStatusRow UI 优化**
- 圆点 5→7px，字体 10→11pt
- hover 背景 `.quaternary`，纵向 padding 1→4pt
- 点击聚焦 pane
- accessibility labels / hints / traits
- 提交：`d5fb73a`

**Pane 拖拽稳定性修复**
- 自定义 UTType `com.openowl.terminal.pane-drag` 防止 `TerminalScrollView` 拦截
- 移除 `draggingPaneID != nil` 条件（解决时序竞争）
- 全路径拖拽日志覆盖
- 提交：`361972c`

**TerminalSearchOverlay 位置修复**
- 从 `topTrailing` overlay 移动到 pane content 内部
- 修复 padding（horizontal 12pt, vertical 4pt）和宽度对齐
- 提交：`361972c`

**AppDelegate 搜索快捷键**
- Return / Shift+Return / Esc 在 search 状态下处理，位于 command guard 之前
- arrow 键导航仅在 `isMultiPane` 时拦截
- 提交：`361972c`

**FileExplorerView 状态更新修复**
- `openFileInTab` 改用 `defer` 延迟调用，避免 view update 期间改状态
- 提交：`361972c`

### 编译状态

BUILD SUCCEEDED（`xcodebuild -scheme openOwl -configuration Debug build`）

### 进行中

- M2/M3 运行时手测（待 Xcode Cmd+R 手测）
- DeploymentStore 拆分（P3，低优先级）

---

## 07:15

### 完成

**Terminal Search 快捷键修复**
- AppDelegate 中 Return/Esc 处理逻辑修正

**Search overlay 布局调整**
- 从 overlay 改为 VStack 内元素

**"Modifying state during view update" 修复**
- isProgrammaticSelection flag + updateData 全块保护

**UTType 声明**
- Info.plist 添加 `com.openowl.terminal.pane-drag`

**Dev 图标修复**
- runtime `NSApp.applicationIconImage` 设置 + 大号 DEV 横幅
- actool 对非 AppIcon 命名的 appiconset 只生成部分 icns → 改用 runtime 方案

**FileExplorer expandDirectory git status**
- 使用 `currentGitContext` 代替 `.empty`

**syncData 机制**
- 展开目录时同步 controller 本地数据

**refreshNow 优化**
- 已有数据时跳过 shallow phase，用 `refreshFullOnly()`

**autoresizesOutlineColumn = false**
- 防止列宽超过 clip view

**updateNSViewController 条件性跳过**
- `controller.rootNodes != store.rootNodes` 才 `updateData`
- @Observable 迁移导致 updateNSViewController 时序变化 → 需要条件性跳过

### 进行中

- Git 角标初始加载位置问题部分修复，FileWatcher 刷新时展开状态可能丢失
- 用户要求 /postmortem 复盘分析

---

## Session 3 — v1.0.2 Triple Fix

### 完成

**Deployment 100% CPU 修复**
- 三层修复：EOF readabilityHandler nil-out、appendLog 节流（200ms buffer flush）、activeStreamIDs 去重避免双重 log poll
- DeploymentPanelView 移除 withAnimation 的自动滚动
- 修改文件：DeploymentProcessManager.swift、DeploymentStore.swift、DeploymentPanelView.swift

**侧边栏分支点击空白页修复**
- listSelection setter 中加 `navigationStore.activeTab = .terminal`
- PaneStatusRow 去掉 onTapGesture 变纯展示组件，整块分支作为一个点击目标
- 修改文件：SidebarView.swift

**终端文件拖拽错误修复**
- TerminalScrollView 四个拖拽方法加入 isEffectivelyVisible 检查
- 拒绝 opacity=0 的不可见终端接收拖拽事件
- 修改文件：TerminalScrollView.swift

**测试**
- 新增 DeploymentLogThrottleTests.swift（12 个测试）
- 覆盖 buffer 累积、flush、100KB cap、activeStreamIDs 生命周期
- 全部通过

**文档**
- 创建 docs/releases/v1.0.2.md
- 创建 docs/postmortem/2026-03-18-v1.0.2-triple-fix.md
- 更新 memory/feedback_swiftui_appkit_hybrid.md 新增规则 5（opacity(0)拖拽）和规则 6（List onTapGesture）

### 决策

- Deployment log throttle 用 200ms coalescing timer（UI 关注点），不在 ProcessManager 层节流
- PaneStatusRow 完全移除交互，由 List selection 统一处理导航
- 拖拽修复在 TerminalScrollView 层做 isEffectivelyVisible 检查（最外层拖拽目标），不在 TerminalNSView 层
